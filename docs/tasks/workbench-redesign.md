# 阅研专业工作台重设计

## 目标与确认决定
用户选择高密度专业工作台；覆盖所有前端页面，沿用暖纸、苔绿、宋体标题，重构布局与公共控件。保留所有已有未提交业务功能，不改变后端 API、鉴权、会话与阅读器数据语义，不发布生产。

## 实施计划
1. 统一视觉令牌与控件：正文14px、聊天15px、控件13px、辅助12px；32/36px按钮、触屏44px热区；独立错误色。
2. 壳层：56px导航、224px历史、弹性正文、320px资料。1280px以上常驻双栏，1024px以上常驻历史，其余按互斥抽屉展开，键盘焦点和SSR安全。
3. 重设对话欢迎区、输入、资料区；列表与文件分类；阅读器外壳；登录、文档、反馈、全部管理页。
4. lint/build、隔离浏览器及PDF夹具回归，1440/1280/1024/768/390截图；同步README和DESIGN。

## 保留行为与验收
保留持久化折叠偏好、草稿、流式思考/工具事件、历史路由、原件下载与来源、PDF文本层几何、选文和笔记恢复、危险操作确认。无意外横向溢出，抽屉互斥且焦点可恢复；不以真实用户数据做截图。

## 进展
- 已检查壳层、令牌、页面、管理控件及浏览器脚本。现有壳层重复折叠轨和固定侧栏造成窄屏挤压，优先修复。
- 已完成骨架与响应式抽屉、视觉令牌、按钮原语、焦点管理、对话欢迎态与输入、阅读书架、文件中心、双区登录、文档/反馈和管理分区。
- 手机文件操作独立成行；抽屉不覆盖桌面偏好；聊天公式复用现有 remark-math/KaTeX，保留安全限制。
- 首轮 lint/build 通过（18页），仅已有 ChatMessage 图片优化警告。全站隔离数据视觉回归通过；PDF夹具回归通过（翻页/缩放/文本选择/翻译/公式/区域标记/笔记/带回对话/草稿/手机/只上传阅读）。
- 最终 lint/build 通过（18页），`git diff --check` 通过；运行数据和生成资源的 `git ls-files` 审计为空。
- 扩展浏览器回归通过：五档宽度、所有管理页面、正常/空/错误、抽屉焦点恢复、危险确认文字门槛、聊天公式/发送/停止/重新生成、下载/文本预览、反馈提交、设置、真实触屏44px热区。无运行时或水合错误。
- 最终截图目录：`/tmp/paper-studio-design-i3esPU/`；代表图 `chat-1440.png`、`chat-touch-390.png`、`login-1440.png`、`chat-messages-1440.png`。PDF截图为 `/tmp/paper-reader-initial.png`、`/tmp/paper-reader-translation.png`、`/tmp/paper-reader-findings.png`、`/tmp/paper-reader-mobile.png`。未在修改前生成基线截图，以上均为改后合成数据截图。
- 主要颜色组合对比度：正文10.45:1、辅助文字4.63:1、主按钮白字5.97:1、错误文字6.23:1；不是对全部内容的自动无障碍认证。

## 交付限制
- 保留已有 `<img>` 优化提示；本轮没有修改图片鉴权/Blob加载行为。
- 启用聊天公式增加其首次加载JS（构建约172→255kB），复用现有依赖，没有新增包。
- 未运行真实模型/生产环境或后端全量测试；本轮未修改后端。测试服务已停止，截图与合成数据留在临时目录。
- 本轮完成，无剩余实施步骤。README与DESIGN已同步。

## 可复现验证
前端依赖使用现有环境，无新增依赖。全站视觉测试拦截全部 API，断言未知 API 请求失败；输出仅合成数据截图到 `/tmp/paper-studio-design-*`。

```bash
cd frontend
pnpm lint
pnpm build
# 已启动本机前端后（默认 http://127.0.0.1:3107）
node scripts/test-workbench-design.cjs
```

PDF验收使用 `tests/e2e/reader_fixture_server.py`（本机8107、临时数据、模拟模型），前端以 `BACKEND_URL=http://127.0.0.1:8107 NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8107 pnpm exec next dev -p 3107 -H 127.0.0.1` 启动，再运行 `node scripts/test-reading-workbench.cjs`。未进行生产操作。
